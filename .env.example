# --- Server Configuration ---
# Set the hostname for your server (e.g., localhost, 192.168.0.50, dev.example.com)
SERVER_NAME=localhost

# --- Port Mapping ---
# Host port for HTTPS. The container port is always 443.
HTTPS_PORT=8443

# Host port for HTTP. Leave empty or comment out if port 80 is unavailable.
# HTTP_PORT=8080